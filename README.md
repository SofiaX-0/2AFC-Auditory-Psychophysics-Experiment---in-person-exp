# Environment

This experiment was developed using:

- PsychoPy standalone v2026.1.1
- Windows 11

The file `psychopy_requirements.txt` contains a snapshot of the PsychoPy Python environment for reference only.

The recommended setup is to install the standalone PsychoPy distribution rather than restoring the environment via pip.